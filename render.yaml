services:
  - type: web
    name: moncv
    env: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /api/templates
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
